{
  "name": "@mhunt/ticketing-common",
  "version": "1.0.0",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "license": "MIT",
  "scripts": {
    "//": "clean tells the del-cli package to wipe the build folder.",
    "clean": "del ./build/*",
    "build": "yarn clean && tsc"
  },
  "dependencies": {
    "del-cli": "^3.0.1",
    "typescript": "^4.0.5"
  }
}
