{
  "name": "@mhunt/ticketing-common",
  "version": "1.0.1",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "license": "MIT",
  "scripts": {
    "//": "don't use this in production.. Just for making life easier for the course",
    "clean": "del ./build/*",
    "build": "yarn clean && tsc",
    "pub": "git add . && git commit -m \"updates\" && npm version patch && yarn build && npm publish"
  },
  "dependencies": {
    "del-cli": "^3.0.1",
    "typescript": "^4.0.5"
  }
}
